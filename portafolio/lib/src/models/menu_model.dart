import 'package:flutter/material.dart';

class MenuItem {
  final String title;
  final IconData icon;
  // final GlobalKey key;

  MenuItem({
    required this.title,
    required this.icon,
    // required this.key,
  });
}
